{"heading":"Adding Data to a Database","explains":{"ExecuteNonQuery()":"This is used to run a command that returns nothing when we execute a SqlCommand","INSERT":"Statement used to add new records to a table"},"exampleDescription":"In this example we are adding a new record to the database using values from the nameAddBox and ageAddBox Textboxes ","example":"SqlConnection sqlConnection = new SqlConnection(@\"...\");\n\nSqlCommand sqlCommand = new SqlCommand(\"INSERT INTO dbo.[Table] VALUES ('\" + nameAddBox.Text + \"', \" + ageAddBox.Text + \")\", sqlConnection);\n\nsqlConnection.Open();\nsqlCommand.ExecuteNonQuery(); // Executes the INSERT command\nsqlConnection.Close();"}